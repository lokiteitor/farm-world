# Dependencies and build output
node_modules
dist
.nuxt
.output
coverage
*.tsbuildinfo

# Generated copies of shared/
backend/src/shared
frontend/app/shared

# Prisma 7 client, generated by `make generate`. It has to live under src/ because
# tsconfig.build.json declares `rootDir: "src"`, and it is regenerable, so it is
# neither versioned nor formatted. Applied by the W2.5 patching window
# (docs/handoff/NOTES-w2d.md, item 2).
backend/src/generated

# Generated by npm
package-lock.json

# Source documents and reports.
#
# The GDD and the stack document are never modified (plan section 2), and the
# rest of docs/ is hand-authored Spanish prose with tables that Prettier would
# reflow. Formatting there is a review matter, not a tooling one.
docs/

# Same argument, applied to the general case instead of one file at a time. A
# README is hand-authored prose wherever it lives, and reflowing the tables of
# shared/api/README.md or backend/prisma/README.md destroys the alignment the
# exclusion of docs/ exists to protect. Applied by the W2.5 patching window
# (docs/handoff/NOTES-w2-cierre.md, item 2).
**/README.md

# Caddy has its own formatter (`caddy fmt`); Prettier does not know the syntax.
infra/caddy/Caddyfile
infra/caddy/Caddyfile.prod
